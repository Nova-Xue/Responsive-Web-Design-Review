transition : box-shadow 1s;

a:hover{
    box-shadow : inset 0 -3px 0 color;
}


.scale:hover{
    transform : sacle(1.4);
}
or scale(0.5);

transform : translate(-20px, -20px); // move the element

translate to center absolute position element

.inner {
    position : absolute;
    top: 50%;
    left: 50%;//based on parent 
    transform : translate(-50%, -50%);//based on itself

}

.rotate: hover{
    transform : rotate (30deg);
}

.skew : hover{
    transform : skew(40deg, 12 deg);
}

matrix

transform-origin

3D transform

transform3d mostly used to trigger side nav bars or anything like that


 
